RewriteEngine On
RewriteBase /
# если файл/папка реально существуют — отдать их
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# иначе — всегда index.html (SPA)
RewriteRule . /index.html [L]
